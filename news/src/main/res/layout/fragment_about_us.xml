<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


dd     <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/us_view"
        android:layout_width="0dp"
        android:layout_height="450dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:listitem="@layout/list_us" />

    <TextView
        android:id="@+id/desciption_us"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_margin="4dp"
        android:textAllCaps="true"
        android:padding="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/us_view"
        app:layout_constraintTop_toBottomOf="@id/us_view"
        android:text=" Liste des fonctionalités : "
     />

    <TextView
        android:id="@+id/lst_us"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_margin="4dp"
        android:textAllCaps="true"
        android:padding="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/us_view"
        app:layout_constraintTop_toBottomOf="@id/desciption_us"
        android:text="Options de consultation, Details des articles, Favoris via une base de données, affichage par pays en fonction des articles disponibles "
      />

</androidx.constraintlayout.widget.ConstraintLayout>